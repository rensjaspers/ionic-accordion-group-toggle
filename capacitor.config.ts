import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-accordion-group-toggle',
  webDir: 'www'
};

export default config;
